Next:
client:
    search by keyword or ID backend
    sort list on front end
    search by keyword or ID front end
    # order-by='column', DESC=on, where=''
        # assert column is correct
    add new item (ID auto generated)
    edit any attribute besides ID
    delete item
live update
add images proper

startup script?
sudo systemctl start mariadb
sudo nginx -t && sudo systemctl start nginx && sudo nginx -s reload
cargo run

support url parameters nicely so people can just copy from the address bar
    that would be cool
